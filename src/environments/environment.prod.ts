//import env from './.env';

export const environment = {
  production: true,
  //version: env.npm_package_version,
  version: '1.0-prod',
  serverUrl: 'https://',
  defaultLanguage: 'en-US',
  supportedLanguages: [
    'en-US'
  ]
};